
{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "routes": [
    {
      "src": "/api/og",
      "dest": "/api/og.tsx"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
