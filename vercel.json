
{
  "buildCommand": "npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install --legacy-peer-deps && npm install -g vite",
  "functions": {
    "api/og.tsx": {
      "memory": 256,
      "maxDuration": 5
    }
  },
  "routes": [
    {
      "src": "/api/og",
      "dest": "/api/og.tsx"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
